# Complex union (conjugate of the mother wavelet) :

If a complex number z' = a' + b'i, so complex union of that is z'* = a' - b'i

ψ = ψ* if it is a real function (without an imaginary number i)

( (x - b)/a ) = z


